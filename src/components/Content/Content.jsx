import { Layout } from 'antd'
import styles from './content.module.scss'

const Content = () => {
  return <Layout.Content className={styles.content}>Content</Layout.Content>
}

export default Content
